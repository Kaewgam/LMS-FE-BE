# **LMS Project \- คู่มือสำหรับนักพัฒนา**

ยินดีต้อนรับสู่โปรเจกต์ Learning Management System (LMS) นี่คือเอกสารที่จะช่วยให้คุณเข้าใจภาพรวม, เทคโนโลยีที่ใช้, และขั้นตอนการติดตั้งโปรเจกต์นี้

## **🚀 ภาพรวมโปรเจกต์**

โปรเจกต์นี้คือระบบจัดการการเรียนรู้ (LMS) แบบ Full-Stack ที่สร้างขึ้นโดยใช้สถาปัตยกรรมสมัยใหม่ โดยมี Backend เป็น Django ที่ทำหน้าที่เป็น REST API และมี Frontend เป็น Next.js ที่ทำหน้าที่แสดงผลและโต้ตอบกับผู้ใช้ทั้งหมด โปรเจกต์ถูกออกแบบมาให้ทำงานบน Docker เพื่อให้ง่ายต่อการพัฒนาและ Deploy

## **🛠️ เทคโนโลยีที่ใช้ (Tech Stack)**

* **Backend:**  
  * **Framework:** Django 5+  
  * **API:** Django Rest Framework (DRF)  
  * **Authentication:** django-allauth, dj-rest-auth, djangorestframework-simplejwt  
  * **Database:** PostgreSQL (แนะนำ) หรือ SQLite3 (สำหรับ Development)  
  * **API Documentation:** drf-spectacular (Swagger UI)  
* **Frontend:**  
  * **Framework:** Next.js 14+ (App Router)  
  * **Language:** TypeScript  
  * **Runtime/Package Manager:** Bun  
  * **Styling:** Tailwind CSS  
  * **State Management:** React Context (AuthContext)  
* **Deployment & Containerization:**  
  * Docker  
  * Docker Compose

## **📂 โครงสร้างโปรเจกต์**

/  
├── backend/          \# โฟลเดอร์โปรเจกต์ Django  
│   ├── lms/          \# Core project folder  
│   ├── lms\_app/      \# Main application folder  
│   ├── Dockerfile  
│   └── requirements.txt  
│  
├── frontend/         \# โฟลเดอร์โปรเจกต์ Next.js  
│   ├── app/  
│   ├── public/  
│   ├── Dockerfile  
│   └── package.json  
│  
├── docker-compose.yml  \# ไฟล์สำหรับรันทุกอย่างพร้อมกัน  
└── .env                \# ไฟล์สำหรับเก็บ Environment Variables (ต้องสร้างเอง)

## **⚙️ การติดตั้งและเริ่มต้นใช้งาน**

### **ข้อกำหนดเบื้องต้น (Prerequisites)**

* Docker  
* Docker Compose  
* Bun (สำหรับรัน Frontend นอก Docker)

### **ขั้นตอนการติดตั้ง**

1. **Clone Repository:**  
   git clone \<your-repo-url\>  
   cd \<your-repo-name\>

2. สร้างไฟล์ Environment Variables (.env):  
   ที่ root ของโปรเจกต์ ให้สร้างไฟล์ชื่อ .env แล้วใส่ค่าตัวแปรที่จำเป็น:  
   \# .env

   \# Django Settings  
   DJANGO\_SECRET\_KEY=your-super-secret-key-here  
   DJANGO\_DEBUG=1

   \# Google OAuth2 Credentials  
   GOOGLE\_CLIENT\_ID=your-google-client-id.apps.googleusercontent.com  
   GOOGLE\_CLIENT\_SECRET=your-google-client-secret

   **หมายเหตุ:** คุณต้องไปสร้าง Credentials ที่ [Google Cloud Console](https://console.cloud.google.com/) ก่อน  
3. รันโปรเจกต์ด้วย Docker Compose:  
   คำสั่งนี้จะทำการ build image และรัน container ของทั้ง Backend และ Frontend ขึ้นมาพร้อมกัน  
   docker-compose up \--build

   * ครั้งแรกอาจจะใช้เวลาสักครู่ในการดาวน์โหลดและติดตั้ง dependencies  
   * หลังจากรันสำเร็จ:  
     * **Frontend** จะอยู่ที่: http://localhost:3000  
     * **Backend** จะอยู่ที่: http://localhost:8000

## **🔌 Backend (Django)**

### **คำสั่งที่ใช้บ่อย (ผ่าน Docker)**

เนื่องจาก Django ทำงานอยู่ใน Container การรันคำสั่ง manage.py จะต้องทำผ่าน docker exec

* **การ Migrate ฐานข้อมูล:**  
  docker exec \-it \<backend-container-name\> python manage.py migrate

* **การสร้างไฟล์ Migration ใหม่ (หลังจากแก้ไข models.py):**  
  docker exec \-it \<backend-container-name\> python manage.py makemigrations

* **การสร้าง Superuser:**  
  docker exec \-it \<backend-container-name\> python manage.py createsuperuser

  **หมายเหตุ:** ก่อนสร้าง Superuser ต้องมีข้อมูล University ในฐานข้อมูลอย่างน้อย 1 ที่

### **เอกสาร API (API Documentation)**

หลังจากรันโปรเจกต์แล้ว คุณสามารถเข้าถึงหน้าเอกสาร API ที่สร้างโดย drf-spectacular ได้ที่:

* **Swagger UI:** http://localhost:8000/api/schema/swagger-ui/

## **🖥️ Frontend (Next.js)**

### **การรันนอก Docker (สำหรับ Development)**

หากคุณต้องการรัน Frontend แยกต่างหากเพื่อการพัฒนาที่รวดเร็วยิ่งขึ้น:

cd frontend  
bun install  
bun run dev

### **Environment Variables**

* NEXT\_PUBLIC\_API\_URL: URL ของ Backend API จะถูกตั้งค่าโดยอัตโนมัติใน docker-compose.yml ให้ชี้ไปที่ http://backend:8000/api  
* NEXT\_PUBLIC\_GOOGLE\_CLIENT\_ID: ใช้ Client ID เดียวกันกับฝั่ง Backend

### **การจัดการสถานะ (State Management)**

โปรเจกต์นี้ใช้ **React Context API** (app/context/AuthContext.tsx) ในการจัดการสถานะการล็อกอินของผู้ใช้, ข้อมูล User, และการทำ Token Refresh โดยอัตโนมัติ ไม่จำเป็นต้องใช้ Redux สำหรับงานนี้

หวังว่าเอกสารนี้จะเป็นประโยชน์สำหรับนักพัฒนาทุกคนในทีมนะครับ\!
"# LMS-F-B" 
